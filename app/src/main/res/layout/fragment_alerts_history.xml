<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <!-- Section header + clear all button -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="@dimen/space_sm">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="RECEIVED ALERTS"
            android:textColor="@color/warm_gray"
            android:textSize="@dimen/text_xs"
            android:letterSpacing="0.15"
            android:fontFamily="monospace" />

        <TextView
            android:id="@+id/alert_history_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0"
            android:textColor="@color/ember"
            android:textSize="@dimen/text_xs"
            android:fontFamily="monospace"
            android:layout_marginEnd="@dimen/space_sm" />

        <!-- Clear all button -->
        <TextView
            android:id="@+id/btn_clear_all_alerts"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Clear all"
            android:textColor="@color/danger"
            android:textSize="@dimen/text_xs"
            android:fontFamily="monospace"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:clickable="true"
            android:focusable="true"
            android:visibility="gone" />

    </LinearLayout>

    <!-- Alert rows injected programmatically -->
    <LinearLayout
        android:id="@+id/alert_history_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical" />

    <!-- Empty state -->
    <TextView
        android:id="@+id/alert_history_empty"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="No alerts received yet"
        android:textColor="@color/warm_gray"
        android:textSize="@dimen/text_sm"
        android:gravity="center"
        android:paddingTop="@dimen/space_md"
        android:paddingBottom="@dimen/space_md"
        android:visibility="visible" />

</LinearLayout>